import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const read=p=>fs.readFileSync(p,'utf8');
test('About Us is upbeat, shared, educational, and humble',()=>{const s=read('src/features/about/AboutPage.jsx');for(const phrase of ['We created EVANGEL','believers and educators','Hebrew and Greek','does not replace Scripture','does not claim to speak for God','Why We Created EVANGEL','Ancient Words, Living Meaning','Our Promise'])assert.match(s,new RegExp(phrase,'i'));assert.match(s,/Concordant/i);assert.match(s,/EvangelMark/);assert.match(s,/SparklingStars/);});