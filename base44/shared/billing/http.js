export function json(data,status=200){return Response.json(data,{status,headers:{'Cache-Control':'no-store'}});}
export function billingError(error){const code=error?.code||'internal_error';if(code==='unauthorized')return json({error:'unauthorized'},401);if(code==='invalid_plan')return json({error:'invalid_plan'},400);if(code==='already_subscribed')return json({error:'already_subscribed'},409);if(code==='billing_not_configured'||code==='stripe_not_configured')return json({error:'billing_not_configured'},503);if(code==='billing_account_not_found')return json({error:'billing_account_not_found'},404);return json({error:'internal_error'},500);}